def pedidos() = 
    result = list.hd(default="", get_process_lines("sudo -u nginx /usr/bin/php /var/www/{{ domain }}/artisan song:dump"))
    request.create(result)
end
laravel = request.dynamic(pedidos)